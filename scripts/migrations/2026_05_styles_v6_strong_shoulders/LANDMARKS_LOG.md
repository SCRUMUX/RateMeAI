# v1.71 Landmark scene-anchor normalization

- Timestamp: `2026-05-21T07:35:54+00:00`
- Mode: `COMMIT`
- Recipes total: 10
- Applied (changed at least one field): 10

## Per-landmark report

| id | lock before → after | overrides before → after | anchor changed | fields touched |
|---|---|---|---|---|
| `athens_acropolis` | locked → semi | 1 → 3 | yes | background.lock, background.overrides_allowed, background_lock, scene_anchor, scene_overrides |
| `barcelona_sagrada` | locked → semi | 1 → 3 | yes | background.lock, background.overrides_allowed, background_lock, scene_anchor, scene_overrides |
| `dubai_burj_khalifa` | locked → semi | 0 → 3 | yes | background.lock, background.overrides_allowed, background_lock, scene_anchor, scene_overrides |
| `london_big_ben` | locked → semi | 2 → 3 | yes | background.lock, background.overrides_allowed, background_lock, scene_anchor, scene_overrides |
| `nyc_brooklyn_bridge` | locked → semi | 1 → 3 | yes | background.lock, background.overrides_allowed, background_lock, scene_anchor, scene_overrides |
| `paris_eiffel` | locked → semi | 0 → 4 | yes | background.lock, background.overrides_allowed, background_lock, scene_anchor, scene_overrides |
| `rome_colosseum` | locked → semi | 0 → 4 | yes | background.lock, background.overrides_allowed, background_lock, scene_anchor, scene_overrides |
| `sf_golden_gate` | locked → semi | 0 → 3 | yes | background.lock, background.overrides_allowed, background_lock, scene_anchor, scene_overrides |
| `sydney_opera` | locked → semi | 1 → 3 | yes | background.lock, background.overrides_allowed, background_lock, scene_anchor, scene_overrides |
| `tokyo_tower` | locked → semi | 0 → 3 | yes | background.lock, background.overrides_allowed, background_lock, scene_anchor, scene_overrides |
